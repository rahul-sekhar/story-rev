<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
  </head>
  <body>
    <p>Thank you for ordering from story revolution! Your order number is #<%= @order.id %></p>
    
    <table>
      <tfoot>
        <tr>
          <td colspan="2"></td>
          <td style="padding:0 2em; text-align:right;"><p><strong>Postage:</strong> <%= RupeeHelper.to_rupee(@order.postage_amount) %></td>
        </tr>
        <tr>
          <td colspan="2"></td>
          <td style="padding:0 2em; text-align:right;"><p><strong>Total:</strong> <%= RupeeHelper.to_rupee(@order.total_amount) %></td>
        </tr>
      </tfoot>
      <tbody>
        <% @order.order_copies.each do |oc| %>
        <tr>
          <td style="padding:0 2em; min-width: 10em;">
            <p><strong><%= oc.copy.product.title %></strong></p>
            <p style="font-style:italic"><%= oc.copy.product.creators %></p>
          </td>
          <td style="padding:0 2em;">
            <p><%= pluralize (oc.number, 'copy') %></p>
          </td>
          <td style="padding:0 2em; text-align:right;">
            <p><%= RupeeHelper.to_rupee(oc.copy.price) %></p>
          </td>
        </tr>
        <% end %>
      </tbody>
    </table>
    
    <% case @order.payment_method.to_i
      when 1
    %>
      <p>Bank Details:</p>
  
    <% when 2 %>
      <p>Address to mail cheque to:</p>
  
    <% end %>
    
    <p><%= @order.delivery_text %></p>
    <% if @order.delivery_method == 2 %>
      <p><%= @order.pickup_point_text %></p>
    <% end %>
    
    <p>We will <%= @order.delivery_method == 1 ? "post your books" : "get in touch with you about delivery" %> once we
    receive your payment. For help or follow up, please reply to this mail.</p>
  </body>
</html>