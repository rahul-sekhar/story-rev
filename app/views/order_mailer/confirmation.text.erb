Thank you for ordering from story revolution! Your order number is #<%= @order.id %>

Items:
======
<% @order.order_copies.each do |oc| %>
<%= oc.copy.product.title %> (by <%= oc.copy.product.author_name %>) - <%= oc.number %> copies - Rs. <%= oc.copy.price %>
<% end %>

Postage: Rs. <%= @order.postage_ammount %>
Total: Rs. <%= @order.total_amount %>

<%= @order.delivery_text %>
<% if @order.delivery_method == 2 %><%= @order.pickup_point_text %><% end %>

We will <%= @order.delivery_method == 1 ? "post your books" : "get in touch with you about delivery" %> once we receive your payment. For help or follow up, please reply to this mail.