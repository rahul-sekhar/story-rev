<% content_for :top_nav do %>
  <li>
    <%= link_to "Books", search_admin_products_path %>
  </li>
  
  <li>></li>
  <li>
    <%= link_to "Search", search_admin_products_path %>
  </li>
  
  <li>></li>
  <li>
    <%= link_to "Edit", edit_admin_product_path(@product) %>
  </li>
  
  <li>></li>
  <li>
    <%= link_to "Book Information", edit_admin_product_path(@product) %>
  </li>
  
  <li>></li>
  <li class="current">
    <%= link_to "Copies", admin_product_path(@product) %>
  </li>
  
  <li class="right">
    <%= link_to "Stock-Taking", admin_products_path %>
  </li>
<% end %>

<div class="input first">
  <label>Title</label>
  <input type="text" disabled="disabled" value="<%= @product.title %>" />
</div>

<div class="input">
  <label>Author</label>
  <input type="text" disabled="disabled" value="<%= @product.author_name %>" />
</div>

<div class="input">
  <label>Editions</label>
  
  <div class="table-wrapper">
    <div class="table-container">
      <table id="edition-table" data-url="<%= admin_product_editions_path(@product) %>" data-publisher="<%= @product.publisher_name %>">
        <% alt = false %>
        <% @product.editions.each do |e| %>
          <tr data-id="<%= e.id %>"<%= alt ? ' class="alt"' : nil %>>
            <td data-val="<%= e.isbn %>"><%= e.isbn %></td>
            <td data-val="<%= e.format_name %>"><%= e.format_name %></td>
            <td data-val="<%= e.publisher_name %>"><%= e.publisher_name %></td>
          </tr>
          <% alt != alt %>
        <% end %>
      </table>
    </div>
  </div>
</div>

<div class="input">
  <label>Used copies</label>
  
  <div class="table-wrapper">
    <div class="table-container">
      <table id="copy-table">
      </table>
    </div>
  </div>
</div>

<div class="input">
  <label>New copies</label>
  
  <div class="table-wrapper">
    <div class="table-container">
      <table id="new-copy-table">
      </table>
    </div>
  </div>
</div>

<%= link_to "Next Book", edit_admin_product_path(@product.next_product), :class => "next-book button" %>
<%= link_to "Previous Book", edit_admin_product_path(@product.previous_product), :class => "previous-book button" %>
<%= link_to "New", new_admin_product_path, :class => "new-book button" %>
